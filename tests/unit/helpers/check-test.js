
import { check } from 'sampleapp/helpers/check';
import { module, test } from 'qunit';

module('Unit | Helper | check');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = check([42]);
  assert.ok(result);
});

